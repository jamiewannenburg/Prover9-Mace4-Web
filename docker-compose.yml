services:
  api:
    build: .
    command: python api_server.py
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
      - PROVER9_API_URL=http://localhost:8000
    restart: unless-stopped
